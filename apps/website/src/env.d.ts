// Committed counterpart to the gitignored, build-generated next-env.d.ts, so
// `tsc --noEmit` resolves Next's static image imports without a prior build.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
